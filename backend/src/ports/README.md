# `ports` Layer

Purpose: interface contracts that isolate app/domain from adapter details.

Allowed imports: `src.ports`, `src.shared`.

Note: currently light usage; new external integrations should define contracts here first.
